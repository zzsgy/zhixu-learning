# 知序本地知识库：项目状态

更新时间：2026-09-16

## 2026-09-16 PDF 单栏正文与表格误判修复

- 修复单栏正文中的宽表格、对照卡片和居中流程图被误当成双栏正文的问题：双栏判定现在要求左右两侧存在足够宽的连续阅读流；页面同时存在多行横跨版心的正文，或主要由稀疏短行表格/流程图构成时，不再拆成左右栏。
- 疑似双栏不再自动转换为整页图片。正常正文继续使用可复制、可搜索的 HTML 文本；只有原有的小字号图内标签密集页和图片碎片异常页才进入原页保真路径，可靠局部图框仍按局部裁剪显示。
- 搜索、目录、AI 问答、全文索引、原始 PDF、数据库正文、分类和历史元数据均不改写。新增“单栏正文加宽表格”“稀疏短行表格/居中流程图”和“普通多栏信号不转整页图片”三类回归。
- 目标 15 页 PDF 逐页验证均为可复制 HTML，第 4 至 14 页不再进入错误双栏或整页图片模式；PDF 专项 11/11、完整回归 215/215 通过。

## 2026-09-15 论文库网格改单列

- 论文库“网格”视图从宽屏两列改为全宽单列，论文卡片按从上到下的顺序展示；筛选区、左侧目录、卡片内部结构、操作按钮和紧凑“列表”视图均保持原行为。
- 单列规则直接作用于论文库局部样式，不修改论文数据、目录、排序或用户保存的视图偏好；新增宽屏单列样式回归，防止以后又被响应式规则恢复成两列。

## 2026-09-15 网页长报告章节遗漏根修复

- 根因是部分长报告把每章放在同一布局容器的独立兄弟区块中；Mozilla Readability 会以正文长度评分选择较长章节，并可能同时排除较短的开头章节和结论。原先只检查“总正文超过 180 字符”，因此剩余内容足够长时仍会错误地保存为成功。
- 网页文章提取现在会对比原始 DOM 与 Readability 的编号标题：只有同一非导航容器中存在从第 1 章开始、编号单调递增的多章结构，且 Readability 命中其中一章却遗漏其它章时，才改用完整章节容器；正常文章不重复补章，导航栏、侧栏和页脚的编号标题不会进入正文。清洗、图片恢复和外部资源安全边界保持不变。
- 实际修复 `Report – AI and Education`（`article_9e04a7cc-a2ab-4c0b-b4c3-95f59b375f35`）：除用户发现的 `1. Introduction`、`1.1 The Landscape` 外，还发现并恢复了原先同样被漏掉的 `4. Conclusion`。英文原文从 75,814 增至 86,077 字符，42 个标题顺序完整；中文全文基于完整原文重新生成，中文与英文均为 42 个标题，翻译状态 `ready/100%`。
- 正式写入前快照为 `zhixu-manual-2026-09-15T02-39-17-421-3ba4335235.db`。按正文、译文和更新时间对比快照，240 篇网页文章总数不变，目标以外 239 篇文章变更数为 0；全文搜索已能命中第一章 `This report is a call to action`。正式服务重启为 PID 15524，健康接口正常。
- 新增“漏开头和结尾章节、已有第一章不重复、导航编号不混入”三类回归，完整测试 211/211 通过；未批量重抓或重新翻译其它文章。

## 2026-09-15 长文档展示规范化深度修复

- 建立 `LONG_DOCUMENT_RENDERING_POLICY.md`，把页眉页脚、英文粘连、列表拆分、标题/目录误判、数据框误判、双栏图表、表格和性能列为统一验收项；正文块采用固定互斥优先级，低置信度一律回退普通段落。
- PDF 阅读提取改为按视觉行和坐标恢复英文空格，并过滤位于顶部/底部 5.5% 页边距的运行页眉、页脚与页码。目标 74 页论文实测不再把 `1 INTRODUCTION 1.7` 当章节标题，也不再显示页尾页码；`actions,` 换行 `while` 在阅读层恢复为 `actions, while`。
- 孤立项目符号与下一正文行安全合并，列表优先于标题和数据块；数据块继续要求多组真实数值特征，普通英文正文始终走段落。复杂 PDF 页仍遵守普通 HTML、可靠局部图框、低可信整页保真的三级路径。
- 所有改动均位于动态阅读提取与浏览器展示层，不改原始附件、数据库正文、搜索索引和历史元数据。部署恢复点为 `zhixu-manual-2026-09-15T02-16-44-115-1741910f1d.db`。

## 2026-09-15 英文 PDF 正文数据框误判修复

- 修复“高密度数据块”把拉丁字母占比当成数据特征的问题：普通英文论文段落不再被逐段套用米色背景和黄色竖线，恢复连续正文阅读。
- 数据块现在必须至少包含 6 组数值，并同时具备明显数字密度、多组百分比、字段分隔或计量单位之一；真正的指标清单仍保留原样式。
- 新增普通英文论文、多指标数据和少量章节编号长正文三类行为测试；专项 11/11 通过。改动只影响浏览器端显示判定，不修改 PDF、提取正文、数据库或搜索索引。

## 2026-09-15 PDF 图表密集页版式修复

- 修复复杂 PDF 页误判：流程图、信息图中的大量 3–6pt 标签以及被 `pdfimages` 拆出的图片碎片，不再作为左右栏正文重新拼接，避免标题、作者、摘要和图中文字交错。
- 新增低可信页原页保真路径：仅在“小字号标签密集”或“图片资源碎片异常多”且没有可靠局部图框时，按需渲染该物理页；普通单栏、正常双栏及已识别局部图表继续使用现有可复制 HTML。正文提取、搜索索引、数据库记录和原文件均不改写。
- 目标文档 `The Last AI Built by Humans: Toward Genuine Recursive Self-Improvement` 的 17 个图表密集页进入原页保真路径，首页标题、作者、摘要、流程图与图注已按原 PDF 完整渲染；其余正文页保持原重排。抽查 `Process Validation` 的 2 个复杂页全部维持原路径，控制阀手册 234 个正常复杂页维持原路径，仅 29 个低可信图形页进入保真显示。
- 新增三类回归：小字号图内标签、图片碎片页、正常正文/可靠局部图框；完整回归 201/201 通过。部署前数据库快照为 `zhixu-manual-2026-09-15T01-29-10-123-f3b07ed9a6.db`，正式服务已重启并通过 `/api/health`；本轮没有重新导入或批量修改任何历史文档。

## 2026-09-14 论文校验回归深度修复

- 撤销结构版本升级时的启动全库重检：服务启动不再重算或改写任何既有论文质量状态。结构重检函数现在必须收到经过审查的明确论文 ID；空目标直接返回，无法再意外作用于全库。
- `papers` 新增 `full_translation_validation_source`：历史记录默认 `legacy` 锁定，新生成译文标记为 `auto`，受控恢复或人工核验标记为 `manual`。自动重检和来源重新提取造成的降级只允许修改 `auto` 记录，`legacy`/`manual` 的状态、清单和说明均受保护。
- 新增受控恢复脚本：快照必须位于知序备份目录；执行前逐篇校验原文 HTML、中文 HTML 的 SHA-256 及翻译完成时间、论文更新时间。任一论文不一致即整批拒绝，事务只恢复两份结构 JSON、校验状态、说明和来源标识，不写正文及时间戳。
- 使用故障前快照恢复全部受影响元数据：5 篇错误降级论文恢复为完整，Mamba 的人工核验说明恢复，另外 11 篇状态未变但被改写的结构计数也恢复。除 Toolformer 的定点修复与快照后完成翻译的 Scaling Laws 外，其余历史论文与故障前结构元数据逐项一致；19/19 篇当前均为完整，正文与时间戳无意外漂移。
- 防回归专项覆盖无目标拒绝、明确 ID 范围、人工记录不可覆盖、正文不一致整批回滚、恢复后正文和时间戳不变；专项 16/16、完整回归 198/198 通过。正式部署前快照为 `zhixu-manual-2026-09-14T09-31-08-875-c1add60bbc.db`（80,310,272 字节），修复后基线快照为 `zhixu-manual-2026-09-14T09-50-46-102-1f006e5aa4.db`（80,306,176 字节），均通过 SQLite 校验。服务再次重启为 PID 1916 后健康正常，重启前后全部论文校验元数据 SHA-256 均为 `edc514a1a01dfe44ec92bb7a6ea7833b1191f8f7ffc5c71c88df68c899207ad0`，确认启动零写入。

## 2026-09-14 论文图文结构校验误报修复

- 修复 arXiv MathML 后备层相邻重复上下标造成的误报：来源正文与中文译文现在使用同一套重复上下标归一化规则，不再出现来源计数 16、译文计数 8 但实际语义结构完全一致的假缺失。
- 结构清单升级为第 3 版计数口径：兼容 ar5iv 把重复脚注标记嵌入多层 `span` 的结构；LaTeX 公式、语义上标、语义下标分别计数，公式总数不再重复包含上下标；读取旧版清单时会先扣除旧口径中已单列的上下标，避免同一差异同时显示为“公式缺失”和“上下标缺失”。
- 原方案曾在服务启动时对所有旧版已完成论文执行一次原地重检；该范围设计导致其他已核验论文的元数据回归，现已由上方深度修复彻底撤销，启动过程不再执行任何既有论文重检。
- 新增旧数据升级回归测试，明确验证误报可恢复为完整、正文和时间戳不变、第二次执行幂等；专项测试 15/15、完整回归 197/197 通过。
- 正式部署前数据库快照：`zhixu-manual-2026-09-14T09-08-19-770-801930697a.db`（80,195,584 字节）。正式服务已重启为 PID 25148，健康接口正常；Toolformer 已从误报的“图文结构降级”恢复为“已校验”，图片 4/4、表格 17/17、LaTeX 公式 175/175、语义上标 8/8、标题 29/29。部署前后原文 HTML、中文 HTML、翻译完成时间和论文更新时间逐项相同，正文 SHA-256 分别为 `d2be0a7ad688570d52d49bb06783f90bb77da818b621cc8d1b16b0f83fe64cce`、`fdc90248422fad66974017947be1f9c69e0f1dd01846ea65cfa1705b79407cf8`。

## 2026-09-14 论文导入限流深度修复

- arXiv 单篇链接导入不再把 Atom 元数据接口的 `429` 直接判为永久失败：Atom 读取异常或返回空记录时，会改读同一篇论文的官方摘要页 `citation_*` 元数据；只有两条官方路径都不可用时才进入后台自动退避。
- 通用导入任务新增持久化 `retry_count` 与 `next_attempt_at`：仅对网络中断、超时、`408/425/429/5xx` 自动重试，尊重 `Retry-After`，否则按 30 秒、2 分钟、8 分钟、30 分钟退避；最多 5 次后明确失败，人工重试会获得一组新的自动重试额度。等待时间写入 SQLite，服务重启后仍会按期续跑，且不阻塞队列中的其他任务。
- 论文卡片和后台任务中心新增“等待自动重试、预计时间、已尝试次数”的明确状态；最终失败时“重试解析”直接显示在卡片操作区，不再藏在“更多”菜单里。永久的格式、权限或内容错误不会自动循环。
- 故障注入验证覆盖 Atom `429`→官方摘要页成功、两条路径同时 `429`、`Retry-After`、服务停止后重新启动续跑、重试上限和人工重置额度；完整回归 193/193 通过。
- 部署前数据库快照为 `zhixu-manual-2026-09-14T08-29-34-209-73652f495f.db`。正式服务已重启为 PID 10364，健康接口与新前端资源正常。失败的 `arXiv 2001.08361` 沿用原论文/任务 ID 后正式恢复成功：任务第 2 次尝试完成 100%，标题和 10 位作者已恢复，原文 12,815 词，arXiv HTML 结构识别为 36 个图片引用、53 张表、46 个标题和 541 项公式；提取错误已清空，中文全文现已进入 Codex 翻译阶段。

## 2026-09-14 三种独立笔记与导出

- “笔记”页新增“＋ 新建笔记”，可创建 Markdown、TXT 纯文本和 Word 富文本；新笔记与原文档、网页文章、论文伴读笔记保留在同一列表，统一搜索、类型筛选、再次编辑和定时整理。
- 新增 `standalone_notes` 表，使用稳定本地 ID 保存类型、标题、可搜索纯文本、编辑器结构及时间；数据库快照和完整备份会自然覆盖该表，没有另建云端存储，也不改变原 `reading_states` 伴读笔记。
- Markdown 提供分栏实时预览和可点击格式工具栏：未选中文字时插入语法骨架并把光标放在继续输入的位置，选中文字时自动包裹；标题、加粗、斜体、项目符号、编号、引用、行内代码、代码块与安全链接都会立即刷新预览。左侧仍保存普通 Markdown 文本，不引入富文本结构或额外依赖；TXT 使用轻量纯文本编辑；Word 提供正文/标题/小标题、粗体、斜体、下划线、左/居中/右/两端对齐、四级增减缩进、项目符号、编号和引用，并采用 MIT 许可 `dolanmiu/docx` 生成标准 `.docx`。
- 阅读工作台的伴读笔记从纯文本框升级为安全富文本编辑器：从网页、Word 或资料正文粘贴时可保留剪贴板实际提供的字体、字号、颜色、标题、粗斜体、列表、引用、代码、上下标、图片、图注、表格及合并单元格等格式；剪贴板实际携带的二进制图片转为有界 Data URL 随笔记保存，知序本地图片统一保留为站内地址。仅携带外站图片地址、未携带图片数据时不主动联网加载，并保留图片说明，避免预览破图、跟踪请求和离线失效。浏览器粘贴前与服务端保存时均按白名单清洗，脚本、事件属性、表单、嵌入网页、危险协议和可加载外部资源的 CSS 会被移除，单条富文本上限 8 MB。
- `reading_states` 新增向后兼容的 `note_html` 字段，原 `note_text` 不改写并继续用于搜索、列表摘要与定时整理；旧纯文本笔记打开时安全转换为段落/标题，后续保存才生成富文本。笔记库的只读“查看”同步展示伴读笔记中的字体、图片和表格。
- 三种编辑器具有 800ms 自动保存、明确保存状态、关闭前等待最后一次保存和同一笔记串行提交；独立笔记可删除，阅读伴笔记仍只能返回原资料编辑。Word HTML 在服务端做标签/属性白名单清洗，脚本、对象、表单、SVG 等主动内容及其内容被丢弃。
- 正式库确认思维导图笔记为 0 条后，移除思维导图的新建、筛选、编辑画布、SVG 生成与导出代码，API 不再接受该类型；数据库旧约束仅保留兼容，未执行破坏性表迁移，也没有删除用户数据。
- 手动点击“保存”会在写入成功后关闭编辑弹窗、刷新笔记列表，再显示成功提示；写入失败时保留编辑器，避免成功提示被遮挡或失败后丢失当前操作界面。
- 每张笔记卡新增“查看”入口，使用独立只读阅读弹窗展示标题、类型、分类、更新时间和完整正文，不显示输入框或格式工具栏；Markdown 与伴读笔记按标题、列表、引用、代码和安全链接排版，TXT 保留原换行，Word 保留清洗后的段落、对齐与缩进。独立笔记可从预览继续编辑或导出，伴读笔记可返回原资料继续阅读。
- 修复笔记模块误用未定义 `--accent` 导致“查看”按钮浅字无底色的问题：笔记页改为有明确浅色/深色前景色的局部主题变量，“查看”“编辑”和“继续阅读”统一使用相同尺寸、底色、字体、阴影及交互状态；浅色模式文字对比度约 4.63:1，深色模式约 7.61:1，同时补齐原来未定义的次要文字颜色来源。
- `/api/notes` 列表扩展为统一笔记接口，并新增独立笔记创建、读取、修改、删除及导出路由；空白 TXT/Word 可保留在列表，但不会生成无内容的定时整理摘要。正式库部署时保持 0 条笔记，没有写入验收数据。
- 完整回归 189/189 通过；新增富文本专项 7/7 通过，图片来源边界收紧后相关组合测试 13/13 通过，验证前后端双重清洗、字体/图片/表格与合并单元格保留、旧纯文本兼容、纯文本搜索副本、富文本只读查看和编辑器节点复用。部署前创建快照 `zhixu-manual-2026-09-14T06-36-48-059-041fa5a1b4.db`，正式服务已重启为 PID 4652、`/api/health` 正常；只读检查为 2 条笔记（1 条独立笔记、1 条伴读笔记），既有伴读笔记仍为原 568 字纯文本且 `note_html` 为空，本轮没有写入验收笔记或删除数据。
- npm 生产依赖审计仍报告 1 项既有高风险传递依赖 `@xmldom/xmldom@0.8.13`，来源是原有 Mammoth / Electron Builder 路径，不来自本次新增的 `docx`；本轮没有自动升级这两个既有模块。改动前恢复点位于 `C:\Users\1、、、\Documents\ChatGPT\知序\standalone-notes-20260914\before`，未提交 Git、推送远端或发布安装包。

## 2026-09-14 独立笔记库与本地定时整理

- 左侧主导航新增“笔记”，集中展示文档、网页文章和论文阅读工作台中的非空伴读笔记；按标题、分类和正文搜索，可按资料类型筛选，并可返回原资料继续阅读。
- 笔记页显示总数、待整理数量、最近记录与最近整理时间；阅读页直接切换到笔记库时，会等待最后一次笔记保存完成，避免刚输入的内容因请求竞态暂时不可见。
- 新增每天/每周可配置的本地整理计划，默认每周日 21:00。任务仅处理上次检查后新增或修改的笔记，以本地规则归纳主题、重点、问题和下一步行动；整理结果单独保存，不改写原始笔记，也不向第三方上传。
- 新增 `note_digests` 派生摘要表和 `/api/notes`、`/api/notes/settings`、`/api/notes/organize` 本机接口；服务每分钟检查一次到期时间，没有新笔记时不生成空摘要。
- 179 项完整回归通过，并在隔离服务中端到端验证笔记汇总、计划修改、立即整理、原笔记不变；正式服务已重启且健康正常。正式库当前尚无非空伴读笔记，因此笔记页显示引导空状态，计划已启用。

## 2026-09-12 本地存储管理页重设计

- 将原先五块等宽说明格改为“存储概况、备份与恢复准备、资料位置、后台任务、浏览器收藏、文件维护”分区。取消大号宣传标题和序号；备份/清理/配对不再混在同一栏。
- 数据库快照与完整备份分别展示范围、时间、大小、校验状态及按钮；完整路径默认折叠，资料根目录/数据库/附件/备份目录独立可选中复制。明确同盘副本无法防止磁盘损坏，不增加自动迁移、网盘同步或恢复执行入口。
- 概况使用现有接口返回的真实快照、备份体积、任务和待清理状态，不伪造磁盘容量/占用率。无备份、未校验、读取失败和空维护队列明确区分；单个存储接口失败不会遮住另一接口成功返回的数据。
- 后台任务保留重点筛选和完整历史，失败项突出显示；列表限定高度，长名称/错误可换行。浏览器新增连接说明折叠，已配对列表常显；无待清理项时禁用清理按钮。备份执行中的按钮显示进行状态并防止重复点击。
- 所有新 CSS 均限定于 `#storage-view`；未改数据库、资料位置、备份机制、权限或任务重试逻辑。完整回归 164/164 通过，新增 4 项界面测试覆盖字段、未知状态、错误文本安全和任务计数。
- 浏览器只读预览以真实数据验证 1920/1280/900 宽度、深浅色、备份路径展开、任务筛选及历史切换，无水平溢出；未触发正式备份、清理、配对或重试。预览阻止非 GET 请求；首次测试端口冲突已通过独立预览端口解决，重新全量通过。
- 改版前源文件保留于 `C:\Users\1、、、\Documents\ChatGPT\知序\storage-style-fix-20260912\source-before`。本次静态界面更新不需要服务重启；未提交 Git 或发布安装包。

## 2026-09-12 论文库列表样式返修

- 用户指出独立目录上线后的列表存在正文居中、左右大面积留白和操作按钮竖排。原因是新 Flex 主体继续继承旧三栏列表的居中对齐与操作区规则，前次视觉验收只覆盖页面顶部，未充分检查卡片正文。
- 论文库使用统一的“选择框＋左对齐正文”结构：分类/来源/日期/目录/阅读状态集中在顶部，标题、英文题名、作者、摘要连续排列；列表摘要两行，卡片摘要三行。
- 常用“中文阅读、移动目录”横向保留；原文、英文 PDF、重试及删除收进“更多”。原操作节点和监听器直接移入菜单，保留原删除确认。支持 Escape 收起、点击外部收起、同一时间只展开一张卡片的菜单。
- 完整翻译状态压缩为轻量提示，悬停可查看原完整说明；结构降级、完整性待核验和失败仍明确显示，不改变任何状态判定。删除重复的大幅宣传区及标题，论文总数改为紧凑摘要。
- 修复新的阅读状态类与正文阅读页同名样式冲突；样式仅在论文管理页面生效，不改正文阅读、目录存储及 API。
- 160/160 自动化测试通过。隔离浏览器验证 1920 宽屏列表/网格、1280 列表及 900 深色窗口：标题/长摘要正常，无横向溢出，常用操作同一行；实际检查“更多”、Escape、勾选与移动弹窗（取消，不移动正式论文）。
- 返修前源文件保留于 `C:\Users\1、、、\Documents\ChatGPT\知序\paper-style-fix-20260912\source-before`。前次新增的多级独立目录和导入继承能力保留；无需数据库迁移或服务重启。

## 2026-09-12 论文库独立目录管理

- 用户确认论文库使用独立目录，不与文档库混用。新增 `paper_folders` / `paper_folder_items`，保留原 `folders` / `content_folders` 与所有论文内容，不自动创建研究主题或替用户分类。既有论文初始显示在“未归档”。
- 左侧多级目录可逐层展开、搜索、显示累计篇数；“全部论文／未归档”、稳定 ID 面包屑、包含子目录开关和本机目录/排序偏好记忆。支持新建根目录/子目录、改名和移动目录，拒绝同级同名、循环及越界目标。
- 单篇或跨页批量移动论文，事务验证整批有效后提交；删除末级目录只将其中论文退回未归档，不删除论文、笔记或进度。有子目录时先移动/处理子目录。
- 论文中文/英文标题、作者、摘要搜索与来源、质量、阅读状态组合筛选；按入库时间、发表时间、标题排序。每页 24 篇，SQL 先过滤后分页，不向列表读取整篇正文；旧 `/api/papers` 调用保持兼容。
- 从论文目录进入导入页自动带入该稳定目录 ID，可手动改选；新 URL 论文与目录关系同事务保存。已有论文重复导入（包括仍在下载的任务）不改变原归档。
- 新增目录/HTTP 回归覆盖 1005 篇分页、跨层级同名、循环保护、整批回滚、删除不丢论文、重启幂等和重复导入归档保留。浏览器使用独立 31 篇测试库验证创建/改名、跨页选择 24 篇并移动、数量不变、导入位置继承。正式数据不作为交互测试材料。
- 完整回归 157/157 通过；独立论文目录是本次新增能力，原有 17 篇论文暂保留未归档，不代表资料丢失。
- 部署恢复点及数据校验记录位于 `C:\Users\1、、、\Documents\ChatGPT\知序\paper-directory-upgrade-20260912`。未提交 Git 或发布安装包。

## 2026-09-12 用户授权的论文去重与完整性修复

- 在完整备份和逐次数据库快照后，合并 5 组重复论文，正式论文库从 22 条变为 17 条；保留 177 个阅读会话、笔记、批注、收藏、标签、目录和 AI 记录。删除的 5 条重复记录可从修复归档或原快照恢复，未删除对应原 PDF 文件。
- 本次原有 5 条结构降级与 1 条完整性未知，去重后涉及 5 篇独立论文。ReAct 修复重复脚注导致的误判；Mamba 恢复 SVG、数据表、公式与作者信息；HNSW 对照全部 13 页 PDF 恢复 26 项图片资产、5 个算法、3 张表和公式；Scaling 恢复 23 个图片引用、16 张表、主要方程、附录和 67 条参考文献；AlphaFold 保留与未知重复项 PDF/译文相同的已核验完整版本。
- 原出版源已有的不完整内容没有编造：Mamba 原图 17 图注残缺，已注明；HNSW 原图 13 图注与面板数量不一致，保留并说明。
- 新增受控论文 PNG/JPEG/WebP 本地图像接口，保留表格下划线语义；公式导出脚本独立化，修复普通括号误识别、xcolor RGB 兼容及摘要公式显示。PDF 缓存键包含渲染器版本、标题、摘要与正文。
- 隔离全回归 153/153 通过。正式服务已重启、健康正常。5 篇相关论文导出页均无破图和 KaTeX 错误；Mamba 正文 558 处、Scaling 正文 597 处 LaTeX 正常渲染。三个翻译队列 list 均为空。
- 修复后完整备份：`D:\ZhixuData\data\backups\zhixu-full-2026-09-12T07-23-52-318-a8ac8cf213`，1936 个文件、1236129504 字节，创建和独立复验均通过 SHA-256；76 个修复论文图片引用及 5 篇原 PDF 已确认在备份中。
- 百度网盘仅完成官方能力核查，尚未连接账号、上传或设置同步。当前备份与正式资料同在 D 盘，不能替代异盘/云端副本。等待用户提供已登录客户端的备份目录或完成账号授权。
- 本轮详细记录：`C:\Users\1、、、\Documents\ChatGPT\知序\知序论文去重与完整性修复记录_2026-09-12.md`；未提交 Git、推送远端或发布安装包。以下“可靠性修复”为本次追加授权之前的历史阶段记录。

## 2026-09-12 可靠性修复

- 上传的记录、搜索索引和目录归属同事务提交；提交后的备份/OCR失败保留原件并明确提示。
- 存储页显示实际数据路径；增加即时数据库快照、完整资产备份与SHA-256验证，以及独立目录恢复模块。备份同盘不能代替异盘副本。
- 统一搜索纳入中文译文和人工名称，提供总数及加载更多；文档/文章列表改轻量SQL分页，界面逐页加载，不再截断在1000项。
- 论文链接先保存占位与持久任务，超时、重启、重试保持同一ID；规范arXiv/DOI身份，保留既有重复记录。质量筛选可定位结构降级、未知完整性和旧重复。
- 外部请求每次重试有独立超时，论文资源逐跳校验公网地址，摘要不再冒充全文；普通论文网页保留图表公式。
- 文章与论文翻译共享持久等待截止时间；额度、登录、临时网络问题保留断点后稍后继续。
- 新阅读活动按本地自然日累积，跨午夜不再整段搬移；旧数据保持原记录口径，不推算未记录的历史分日时长。
- 普通API增加本机来源验证；删除与待清理文件队列同事务提交，文件占用时明确提示并重启/定时/手动重试。
- 验证与部署详情见 `REPAIR_STATUS.md`；本轮部署后完整回归145项通过，已重启并核对正式服务。没有自动合并旧重复、重译现有论文或清理仍可能被引用的文章图片。

## 当前定位

知序是仅供个人使用的 Windows 本地知识库。网页服务监听 `127.0.0.1:47821`，正文、阅读记录、论文、项目研读档案、问答记录和目录结构保存在本机 SQLite；当前电脑的数据根目录是 `D:\ZhixuData\data`，视频运行目录是 `D:\ZhixuData\video-runtime`。

当前只考虑电脑端，不包含账号、多用户和远程云数据库。

## 已完成模块

- 文档库：本地文档上传、网页文章解析、文件夹分层、移动、收藏、删除、搜索。
- 阅读页：目录侧栏、阅读工作台、字体和行距调整、批注、文章内 AI 问答、悬浮返回；支持“知序经典、沉浸夜读、柔和纸张”三种独立记忆的阅读样式。沉浸夜读采用本地随项目提供的霞鹜文楷正文、深蓝黑纸张和宽屏右侧本页目录，并在阅读期间统一覆盖应用底色、左侧导航、悬浮按钮、目录和工作台；柔和纸张使用暖白低对比文楷。超长文章标题会自动降低字号；公众号等来源中依赖原站裁剪的超高窄幅章节拼接素材和重复小装饰会在加载后自动剔除，并清理失去内容的空包装层，原有编号标题恢复为独立蓝色标题条，正常内容图、单张纵向信息图和二维码不受影响。文档、网页文章和论文中的代码块统一使用居中、细边框和宽留白的阅读卡片，提供轻量语法着色、横向滚动和仅在悬停或聚焦时出现的复制入口，行内代码显示为等宽胶囊；富文本错误拆成相邻 `<code>` 节点的目录树会在新导入时规范为真实换行，旧文章打开时也即时兼容恢复，不需要重新导入。以“提示、注意、警告、示例、结论”等中英文字段开头的特殊文本自动转为对应色彩的提示块，并继承当前阅读主题的正文颜色和字体。三种样式离开文档、文章或论文阅读页后均恢复知识库管理界面的原有配色。
- 本地文档章节阅读：Word、PDF 及其他可解析文件默认使用站内正文页；Word 优先按 H1/H2 并保留内嵌图片，PDF 优先读取文件内嵌原生书签及其物理页码，没有书签时才按“第 X 章”或 1、1.1 等正文标题回退识别。沉浸夜读中的 PDF/纯文本详情采用居中单列正文，文档分类、文件信息和 OCR 状态压缩为正文上方的信息条，不再由固定右栏挤窄正文或在宽屏右侧留下大面积空白。PDF 解析会检查“页面可正常绘制、但 Unicode 文字映射损坏”的伪文字层；命中大量控制字符、私用区字形和异常扩展字符的组合信号后，不再让乱码进入摘要、搜索与阅读页，而是自动转入本地 OCR。OCR 完成后的分页正文在阅读详情中优先于原 PDF 文字层，避免再次被坏映射覆盖。PDF 解析同时记录逐页文字坐标、多栏间隙、连续编号图例及内嵌图片数量：普通单栏页面继续使用可重排 HTML；多栏、爆炸图/编号引线、多图混排等复杂页面使用可选择、可复制的双栏 HTML 重建。正文仍是 HTML；工艺图只按图注边界局部裁剪原 PDF，完整保留叠加在底图上的矢量编号圆点、引线、图例和图注，不再把整页栅格化。图中已保留的孤立数字标号不会再次落入页尾正文。左右栏顺序、页眉和页码保持对应，扁平文本仍进入搜索与 AI 数据层但阅读页不重复显示。异常的乱序孤立书签会自动排除，表格数值和单位不再误判为目录标题。带编号题注的矢量表格仍可依据 PDF 文字坐标进行局部高清裁剪。左侧显示整本文档章节，正文单次只渲染当前章。超长章节继续按安全字数拆成 1/2、2/2，无法识别标题时按固定长度分页；顶部和底部均可切换上一章/下一章，阅读进度按整本文档保存。PDF 原版不再嵌入知序页面，而是由“↗ 新标签页查看原版”直接交给 Chrome/Edge 原生 PDF 阅读器。
- PDF 长文阅读体验：阅读页统一为“左侧章节目录 + 居中单列连续正文”结构。目录默认展开，阅读工作台默认收起；全部章节沿同一条正文流连续向下阅读，不按小节分页，也不显示上一章/下一章。章节分批创建，并通过浏览器原生 `content-visibility` 跳过离屏章节的布局与绘制；不再手工卸载正文或动态修改占位高度，避免滚动锚定导致页面停滞。目录点击直接定位，滚动时通过二分定位同步高亮。复杂双栏页在 HTML 阅读中按左栏后右栏转换为单列文章，去除白色 PDF 纸张卡片；低置信整页保真标记只保留可复制文字与原版入口，不再把整页栅格图插入正文，真实局部图框、表格和公式继续保留。巨幅文件名、摘要碎片、技术元数据和右侧信息卡不进入阅读流，原始 PDF 可从顶部操作栏在新标签页打开。
- 教程导入：支持 Docsify/GitHub docs 目录识别与批量章节导入。
- 论文库：经典论文推荐、PDF/链接导入、英文原文保存、Codex 中文翻译队列。
- 资料问答：基于本地资料检索、引用核验、对话历史；当前左侧入口隐藏，代码和既有记录保留。
- 项目研读：输入公开 GitHub 仓库主页链接，按顺序采集项目元数据、README、目录树、主要贡献者、最新版本和关键配置文件，并基于这些可核验证据生成简体中文项目画像、架构、核心模块、技术栈、执行链路、优势风险、上手顺序与学习建议。即使仓库资料全部为英文，项目名、代码和文件路径以外的自然语言说明也必须使用中文；首次结果中文比例不足时自动执行结构化中文转换，仍不合格则拒绝保存。报告和关键统计保存在本机 SQLite；未配置 DeepSeek 时降级为不虚构细节的本地概览，可选 `GITHUB_TOKEN` 仅在本机服务端用于提高 GitHub API 访问额度。
- 本地运维：SQLite 每日一致性备份、服务守护进程和 Windows 登录自启动任务；自启动入口优先使用正式 Node.js，未安装系统 Node 时回退到项目自带 Electron 运行时，不依赖 Codex 临时运行时路径。守护日志按跨日或 5 MB 上限自动轮换，归档保留 30 天。
- Windows 桌面版：通过 Electron 复用现有本机 HTTP 服务和同一份 SQLite 数据，提供品牌启动页、单实例窗口、系统托盘、中文菜单、本机健康检查和服务按需启动；外部链接交给系统浏览器，桌面网页内容保持 Node 隔离。开发版沿用现有数据配置；正式安装版把默认数据库、附件、私密环境配置和日志放在当前 Windows 用户的应用数据目录，安装包和 Git 均不包含本机知识库、密钥或运行记录。Electron Builder 可生成支持自选安装目录、桌面快捷方式和开始菜单入口的 x64 Windows 安装程序。
- 第五批第一阶段：统一后台导入任务、任务恢复与失败重试、Chrome/Edge 快速收藏扩展、一次性配对和客户端撤销。
- 浏览器验证网页收藏：普通后台抓取遇到 HTTP 403 时明确提示改用快速收藏；扩展 0.2.1 在用户主动点击后读取当前标签页 HTML，供本机服务按同一公网 URL 校验、15 MB 容量限制和正文安全清洗流程解析，兼容 Cloudflare 等只能在真实浏览器中打开的公开文章。完整 HTML 不写入持久化任务参数，扩展不读取 Cookie、密码或浏览器存储。
- 网页文章图片稳定缓存：同一图片的首次并发请求共享一次安全下载和磁盘写入，进程外缓存竞争安全复用；阅读页遇到瞬时加载失败会自动重试一次，避免新导入文章首次打开时长期显示破图。
- 网页文章异常媒体防护：导入时识别并清理紧凑的关注、扫码、二维码和商务合作推广块，只把原图固定像素宽度转换为安全显示提示，不恢复来源网页的任意样式；依赖已剥离父容器的百分比宽度只作为结构标记，孤立的小型角标和曲线按自然尺寸靠右显示。阅读旧记录时再次执行同类清理，所有正文图片统一限制在一屏左右并保持比例，避免问号动图、竖长装饰和失去裁剪规则的素材撑坏阅读页。
- 网页文章安全样式模型：来源页面的任意 CSS 仍全部拒绝，只把可验证的纯色背景、边框、圆角、内边距、对齐、Flex 标题行、强调色和小字号转换成知序自有语义类；浅色内容面板自动映射当前阅读主题。富文本编辑器把 14px 长正文误写为 `h1` 等标题时，会依据显式字号、文本长度和句子结构降级为普通段落，避免正文被放大成标题，同时保留真正的短章节标题。
- 网页文章媒体保真：解析前为正文中的大图建立位置锚点，恢复 Readability 遗漏的 Framer 等布局图片，并可按正文块位置把修复后的原文图片补入既有中文译文；正文中的 YouTube 链接保存为受控视频元数据，阅读页只在用户点击后加载 `youtube-nocookie.com` 播放器，同时保留原视频入口。
- 文档库展示名称：本地文档和网页文章均可从列表修改名称；人工名称与原文件名、来源标题分开保存，并同步列表、搜索结果、阅读页和全文索引，后续重新解析来源时不会覆盖人工名称。
- 第五批第二阶段：图片及扫描 PDF 本地 OCR、分页正文与坐标、置信度、自动入队和阅读页重试。
- 第五批第三阶段：YouTube/哔哩哔哩视频链接、公开字幕优先提取、时间戳正文、无字幕等待确认与仅保存链接流程。
- 第五批第四阶段：无独立字幕轨时，经用户逐条确认后本地临时下载视频，完成语音转写、关键画面提取、画面 OCR 与时间轴对齐，并生成可搜索图文 PDF；任务结束自动删除临时媒体。
- 第五批第五阶段：视频入口统一生成图文 PDF 并作为正式文档进入文档库；有公开字幕时直接用于时间轴对齐，没有字幕时才本地转写。视频运行资产和临时目录迁移到 D 盘。
- 视频 PDF 精简：移除画面 OCR 文本区及逐帧 OCR 步骤，只保留关键截图、对应讲解与原视频时间戳。
- 后台任务中心精简：活动和失败任务优先，已完成任务默认只显示最近 5 条；支持状态/类型筛选、全部历史切换，并通过固定高度滚动避免页面无限向下延伸。
- 论文库状态修复：论文列表改用正文词数和全文翻译状态判断可读性，避免接口省略大段正文后误显示为“正在后台下载并解析”。
- 每日经典全文校验：内置经典目录必须提供公开来源页与直接 PDF 地址；目录元数据修正会同步到当天候选和已经选入论文库的记录。缺少 PDF 且没有正文时立即记录明确的全文缺失错误，列表与阅读页不再无限显示“等待 Codex 翻译”。出版商 PDF 若向后台返回验证页，系统会自动改从同一公开文章页提取完整正文。AlphaFold 经典论文已补充 Nature 官方公开 PDF，并可从 Nature 开放文章页后备提取全文。
- 论文图文保真翻译：优先使用 Nature 等出版商开放正文和官方 arXiv HTML，按图注补回 Readability 漏掉的正文主图，把 MathML 的可验证 TeX 注释转换为本机 KaTeX 公式，并在分段翻译前用不可改写锚点保护图片、LaTeX 与 HTML 上下标。论文记录保存原文 HTML、图片/公式/表格/章节结构清单和译文结构清单；写回前执行完整性门禁。只有 PDF 文字层且无法可靠恢复图文结构时明确显示降级提示，不再把字符数达标等同于图文完整。
- 论文版面语义保真：arXiv 正文中的安全 SVG/图片 `<object>` 会先转换为受控图片，嵌套图形按唯一媒体资产校验，带题注却没有图表内容的空图会阻止来源误报完整；译文保留并约束表格的 `colspan`/`rowspan`，旧译文可依据相邻列数恢复丢失的跨列表头。论文附录中的 Question/Thought/Action/Observation 等提示轨迹统一渲染为紧凑字段组，宽表格保留可横向查看的原始列关系，分页续接提示和图表题注使用独立语义样式，重复上下标会去重；以上规则同时用于网页阅读和中文 PDF 导出，不再把论文轨迹误套普通代码卡片或普通正文间距。
- 论文翻译队列可靠性：Codex 用量达到上限时把当前论文退回等待队列并低频自动重试，不再把额度问题批量记成论文失败；每个分段写入缓存前逐一核对图片和公式锚点，漏写或重复时仅自动重试该分段，单篇内容异常也不会阻塞后续论文。
- 论文库视图：支持网格与紧凑列表切换，列表集中展示标题、作者、摘要、来源、日期、翻译状态和操作入口，并单独记住上次选择。
- 中文论文 PDF：已完成且通过结构完整性门禁的中文论文可使用本机 Chrome 生成 A4 PDF；缓存按译文哈希复用，译文更新后自动失效，打印页会移除脚本等非阅读内容。
- 列表中文简介：英文网页文章在文档库中优先展示 Codex 已保存的中文简介；英文论文优先展示中文摘要，没有独立摘要时从已完成的中文全文提取首个正文段。尚未完成翻译的条目仅显示中文处理状态，不再在标题后直接铺出英文简介。
- 上传反馈精简：文档上传成功后改用右下角短暂通知，成功状态行自动收起且空队列不再占位；上传中和失败状态仍留在页面，便于观察和处理。
- 文件夹批量导入：导入页在 Chrome/Edge 中优先使用原生目录选择器，旧浏览器回退到 `webkitdirectory`；通过受控相对路径在知识库中建立或复用同名目录树，常见临时文件自动跳过，批次使用总进度显示，成功结果自动收起、失败文件集中保留。
- 导入查重：普通文档按规范化原标题和 SHA-256 原文件摘要查重，网页文章按最终网址、规范化原标题和正文查重；单文件重复时明确提示，文件夹批量导入静默跳过重复项并在结束后集中列出文件名和命中原因。
- 上传目录选择：单文件、多文件和文件夹批量导入均可选择系统自动识别，或在上传前指定现有知识库目录；指定目录下仍保留本机文件夹的相对层级。
- 导入中心界面：本地文档、论文、网页文章和视频统一为紧凑的 2×2 卡片布局，缩短说明、输入区和按钮高度；中窄屏自动切换单列，原有目录选择、批量上传、查重与后台任务逻辑不变。
- 文档库完整列表：文档和网页文章列表接口一次返回上限统一提升到 1000 条，避免资料总量超过默认 200 条后，文件夹卡片显示数据库完整数量、打开目录却只渲染最近一部分内容。
- 批量移动：文档库提供批量整理模式，可勾选多项文档、网页文章或统一搜索结果，再通过数据库事务一次移动到目标目录。
- 批量删除：批量整理模式可永久删除当前勾选的多项内容；执行前显示数量和标题摘要并再次确认，服务端通过单一事务清理正文、索引及关联记录，随后删除对应原始附件，任一目标无效时整批拒绝。
- 文件夹移动：文件夹卡片可把整个目录树移动到另一目录下或知识库根目录；服务端拒绝移动到自身、后代目录和同级同名目录，原有内容归属与累计数量随目录树完整保留。
- 移动目录选择器：单项、批量内容和整个文件夹共用可逐级折叠的目录树，默认只展开来源路径，支持按名称或完整路径搜索、全部折叠，并可在弹窗内直接新建一级目录或选中目录的子文件夹。
- 文章翻译自动化：英文文章加入队列后由本机 Codex 分段翻译；解析器会把旧博客误用的非 SVG `<image>` 规范化为标准图片，原文图片再通过不可翻译锚点保持在对应段落和图注位置，页面经本地图片代理展示完整图文译文；阅读页显示队列位置、真实完成分段、阶段和百分比，失败可重试，服务重启后从数据盘中间结果继续；文章和论文翻译中间文件统一存入知序数据盘并在成功后删除。
- 图文翻译会在分段前拆除 `<figure>` 外壳，把图片锚点与可翻译图注转换为普通语义段落，避免整块图片被正文分段器遗漏；格式升级时旧断点会自动失效并重新生成。
- 数学公式显示：翻译前先用不可改写锚点保护 LaTeX 源码，英文原文、中文译文和双语对照再统一使用本机 KaTeX 渲染 `$...$`、`$$...$$`、`\\(...\\)` 与 `\\[...\\]`；公式脚本、样式和字体均从本地服务提供，代码块不会被误处理。
- 学习与资料统计：作为刷新后默认首页并位于文档库之前，可切换最近 7、30、90 天和 1 年；展示活跃阅读时长、阅读内容数、活跃天数、新增资料数、阅读时长折线、阅读进度环形分布，以及可直接继续打开的最近阅读和最近入库列表。资料库内容统计使用分层横向条形图：论文库单独统计篇数，文档库按一级目录汇总全部后代、按二级目录缩进展示，并区分文档与网页文章数量；展开二级目录时，每个一级目录及其直属二级目录由交替浅底色、分组边框和底部一级目录名称组成连续分组，论文库则明确标为独立统计。可见柱状项会在数量允许时均匀铺满图表宽度，项目过多时保持最小列宽并横向滚动，避免右侧出现大面积无效留白。项目研读统计补充 GitHub 档案数、近 90 天仍活跃的项目、累计 Star/Fork、主要语言分布与最近研读入口。既有阅读状态用于回溯历史进度和时间节点；精确阅读时长从功能启用后，通过可见页面、近期交互与幂等阅读会话累计，不伪造历史时长。统计页整体改为低饱和薄荷色、圆角纸张层次和更清晰的图表留白。
- 工作记录编辑：工作台“工作记录”目录可新建和编辑 Markdown 记录，标题、正文、提取文本和全文索引同步更新，其他普通导入文档仍保持只读来源语义。

“专题”和“卡片复习”入口目前隐藏，数据结构和代码仍保留。

## 重要目录和文件

- `server.mjs`：本地 HTTP/API 服务与静态资源响应。
- `desktop/main.mjs`：Windows 桌面窗口、托盘、菜单和本机服务启动编排。
- `desktop/splash.html`：桌面版本地品牌启动页。
- `public/index.html`：页面结构。
- `public/app.js`：浏览器端交互与状态管理。
- `public/styles.css`：界面样式。
- `lib/database.mjs`：SQLite 表结构、迁移和数据访问。
- `lib/article-parser.mjs`：网页抓取、代理、正文解析和安全清洗。
- `lib/article-video.mjs`：从网页正文识别、去重并规范化受支持的视频元数据。
- `lib/log-rotation.mjs`：守护日志的跨日/大小轮换与超期归档清理。
- `lib/github-project-service.mjs`：GitHub 公开仓库链接校验、证据采集与结构化中文研读。
- `lib/paper-fulltext.mjs`：论文 PDF 下载、缓存和全文提取。
- `lib/codex-paper-translator.mjs`：Codex 论文翻译工作器。
- `lib/import-job-runner.mjs`：OCR、视频和浏览器收藏共用的持久化后台任务执行器。
- `lib/ocr-service.mjs`：Tesseract 图片识别、扫描 PDF 页面渲染和 TSV 版面结果解析。
- `lib/video-importer.mjs`：视频链接规范化、平台元数据、公开字幕选择与时间戳正文生成。
- `lib/video-study-service.mjs`：明确确认后的本地视频处理编排、工具检测、临时文件清理和图文 PDF 结果管理。
- `scripts/video-study-export.py`：faster-whisper 转写、关键帧筛选、Tesseract 画面 OCR、时间轴对齐与 PDF 排版。
- `browser-extension/`：仅连接本机知序服务的 Chrome/Edge 快速收藏扩展。
- `scripts/`：翻译队列及运维脚本。
- `scripts/run-service.ps1`：为计划任务选择稳定 Node.js 或项目 Electron 运行时。
- `service-runner.mjs`：监督本机服务、异常退避重启并写入轮换日志。
- `work/`：运行日志和任务状态。

## 外部网络规则

外部网页和论文请求必须通过 `EnvHttpProxyAgent` 读取 `HTTP_PROXY`、`HTTPS_PROXY` 和 `NO_PROXY`。当前电脑代理地址为本机代理，Node 原生 `fetch` 不会自动读取这些变量，因此不能直接用于外部资源。

抓取器保留 Node 默认根证书，并补充 Let’s Encrypt 官方 Generation Y 中间链，兼容漏发 YR1 链的网站；HTTPS 主机名、有效期和签名校验不会关闭。

公网 URL 安全校验会对 `ENOTFOUND`、`EAI_AGAIN` 和 DNS 超时执行有限重试；持续失败时返回中文提示，同时仍拒绝本机、私网和保留地址。

普通网页按解压后的 HTML 计算容量，硬上限为 15 MB，以兼容 Jupyter/Quarto 导出的长篇代码与公式教程；超过上限仍会在进入 DOM 解析前拒绝。

Notebook/Quarto 中合法的 Base64 PNG、JPEG、GIF、WebP 会先校验文件签名和 12 MB 单图上限，再按内容哈希写入 D 盘文章图片缓存；SQLite 正文只保存受控虚拟地址，阅读页与译文继续通过本地图片代理加载。

论文 PDF 导入采用异步流程：先保存论文记录，再在后台下载、解析、分类并唤醒 Codex 翻译。失败原因写入 SQLite，论文卡片提供重试入口。

## 开发与验证约定

- UI/文字/样式小改：运行 `node --check public/app.js` 和相关页面定向检查。
- 导入与解析：运行 `npm run test:import`。
- 数据库和持久化：运行 `npm run test:database`。
- 页面/API 集成：运行 `npm run test:ui`。
- 浏览器收藏和后台任务：运行 `npm run test:browser`。
- 视频链接、字幕解析和确认流程：运行 `npm run test:video`。
- 视频图文 PDF：除自动化测试外，使用真实视频完成端到端生成，并用 Poppler 渲染全部页面检查版面。
- 版本阶段完成或高风险修改：运行 `npm test` 全量测试。

本地静态资源使用 `Cache-Control: no-store`，修改后普通刷新即可，不再依赖手工版本号解决缓存混用。

## 2026-09-10 维护收口验证

- 当前自动化测试共 78 项，已完成全量验证，结果为 78/78 通过；中文论文 PDF 与 OCR 测试使用独立端口，避免并行运行时连接到错误测试服务。
- Windows 计划任务 `ZhixuLocalKnowledge` 已重新安装，动作指向 `scripts/run-service.ps1`，采用当前用户登录触发、受限权限和单实例策略。受控终止服务子进程后，`service-runner.mjs` 已在 1 秒后自动拉起新进程，`/api/health` 恢复为 `status=ok`。
- 三类 Codex 翻译队列（文章、论文全文、论文候选摘要）均为空；已完成内容没有重复入队。旧失败导入任务在导出审计记录后清理，用户主动取消的文章翻译状态仍保留。
- 旧服务日志已经按日期归档，新日志从空文件继续记录；后续由自动轮换和 30 天保留策略控制增长。
- 第二物理介质备份尚待确定外接硬盘、NAS 或另一台电脑的目标路径；现有 D 盘每日 SQLite 备份继续生效，但不能替代异盘恢复副本。

## 2026-09-11 论文版面修复验证

- ReAct 的手动导入记录与每日经典重复记录均已重新提取 arXiv HTML：来源结构现可核验 6 个唯一 SVG 图片资产、10 张表、34 个章节标题和 56 项公式/上下标结构；旧中文译文已补回全部唯一图片、恢复跨列表头并清理重复上下标。由于旧译文本身仍缺少少量公式与上标，状态已从错误的“完整”纠正为“降级”，未伪造缺失内容。
- 浏览器实页已核对附录提示轨迹与 WebShop 表 6：Question/Thought/Action/Observation 的中文同义标签均进入紧凑字段行，表头跨两列，Act/ReAct 保持左右对照；本机守护进程受控重启后自动拉起，`/api/health` 返回 `status=ok`。
- 当前自动化测试共 96 项，已完成全量验证，结果为 96/96 通过；其中包含真实 Chrome 中文论文 PDF 生成、SVG `object` 转换、嵌套图形资产计数、空图完整性门禁、表格跨行跨列、旧表头恢复、提示轨迹和分页提示的回归测试。

## 当前待办

- 第六批后续：继续积累学习事件，再实现知识关联和主题路线；当前已先完成不依赖历史时长推测的学习统计页。
- 多端同步第一阶段（暂不实施）：保留电脑端 SQLite 与附件为主库；先同步小说/笔记等小型结构化内容、目录、标签、阅读进度和批注，附件采用按需下载与 SHA-256 去重。通过受认证的同步 API 连接云端元数据和对象存储，不能用网盘直接同步 `zhixu.db`、WAL 或附件目录；需提供设备身份、版本号、冲突副本、软删除恢复和完整备份恢复验证。
- 文档库入口重组（第一阶段已完成）：一级入口为“工作资料、工作台、学习、待整理”；既有 `自控`、`生物工程`、`工艺工程`、`安全` 归入工作资料，`AI`、`数据库`、`程序`归入学习，`其它`归入待整理；工作台预置“项目、工作记录、交付物”空入口。今后未指定保存位置的导入只进入“待整理/专业分类”，不替用户猜测工作、学习或项目用途；用户指定目录优先。后续再提供批量整理、入口说明和小说独立模块。
- Android 端暂不在当前范围。

## 视频隐私与本机资产

- 不读取、导出或保存浏览器 Cookie；登录态只用于用户在浏览器中人工确认页面状态。
- 视频入口点击后会再次说明并确认临时媒体处理；成功、失败和服务异常重启后都会删除临时视频、音频、截图及未入库 PDF。
- FFmpeg、yt-dlp、本地 Python 环境、Whisper 模型和临时媒体位于 `D:\ZhixuData\video-runtime`，通过 `.env.local` 的 `ZHIXU_VIDEO_RUNTIME_DIR` 配置，不提交到 Git。
- 最终图文 PDF 存入文档附件目录并建立 SQLite 文档、分类、文件夹和全文检索记录，不在视频暂存目录保留重复副本。
